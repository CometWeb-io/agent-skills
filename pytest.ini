[pytest]
# Default (prepend) import mode is required: several skill tests import a
# sibling test module by bare name, which only resolves when pytest puts the
# test directory on sys.path. That makes test basenames a repo-wide namespace,
# so keep them unique across skills.
testpaths = tooling/tests skills
