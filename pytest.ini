[pytest]
# importlib mode lets two skills ship tests with the same basename
# (e.g. test_quality_regressions.py) without colliding at collection time.
addopts = --import-mode=importlib
testpaths = tooling/tests skills
