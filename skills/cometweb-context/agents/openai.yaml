interface:
  display_name: CometWeb Context
  short_description: Build source-backed, current CometWeb context
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
  default_prompt: Use $cometweb-context for the requested task; preserve scope, evidence, and verification
    limits.
policy:
  allow_implicit_invocation: true
