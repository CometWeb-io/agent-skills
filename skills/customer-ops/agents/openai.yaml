interface:
  display_name: Customer Ops
  short_description: Track customer cases through verified resolution
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
  default_prompt: Use $customer-ops for the requested task; preserve scope, evidence, and verification
    limits.
policy:
  allow_implicit_invocation: true
