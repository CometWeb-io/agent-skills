# Rubric lock

When the workflow uses a named quality policy, preserve `pack_id`, `revision`, canonical SHA-256, and `locked_before_execution=true`. Changing the rubric after work starts is a material brief delta, not an in-place edit. Revalidate affected downstream stages.
