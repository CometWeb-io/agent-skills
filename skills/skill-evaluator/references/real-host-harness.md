# Real-host harness

Prefer an actual user-facing agent harness rather than a raw model API when evaluating skill discovery. Record exact CLI/harness version, model, reasoning effort, permissions, workspace setup and effective configuration.

If authenticated execution is unavailable, validate the spec and stop at `DESIGN_READY`. Do not simulate pass@k, latency, tokens or trigger rates.
