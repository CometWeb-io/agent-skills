# External results

Import real-host results only when candidate/baseline identity, rubric hash, benchmark hash, host/model/harness config, run count and grading provenance match the frozen experiment. Mismatches invalidate direct promotion comparison.
